/* exported take */
// define a function named take with two parameters array and count
// call the method slice of the object array and set it to the parameter count minus 1
// assign the result to the variable result
// return result

function take(array, count) {
  var result = array.slice(0, count);
  return result;
}

// define a function named take with two parameters array and count
// call the method slice of the object array and set it to the 0 andparameter count
// assign the result to the variable result
// return result
