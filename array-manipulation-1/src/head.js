/* exported head */
// define function named 'head' with one parameter 'string'
// return the array at the index 0

function head(array) {
  return array[0];
}

// define function named 'head' with one parameter 'string'
// return the array at the index 0
