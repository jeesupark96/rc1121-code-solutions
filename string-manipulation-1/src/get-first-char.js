/* exported getFirstChar */
function getFirstChar(string) {
  var first = string.charAt(0);
  return first;
}

// define a function named 'getFirstChar' which has a single parameter 'string'
// call the charAt method to get the first element in the string and assign it to the variable named 'first'
// return the variable first
