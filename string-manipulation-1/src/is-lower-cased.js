/* exported isLowerCased */
function isLowerCased(word) {
  if (word === word.toLowerCase()) {
    return true;
  }
  return false;
}

// define the function named 'isLowerCased' called with one parameter 'word'
// call an if statement with one condition, if word is strictly equal to the result of the toLowerCase method of word then the function will run.
// if the function is true, return true
// if the function does not run true, return false
