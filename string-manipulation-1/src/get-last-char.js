/* exported getLastChar */
function getLastChar(string) {
  var last = string.charAt(string.length - 1);
  return last;
}

// define a function named 'getLastChar' with one parameter 'string'
// call the charAt method with one argument,the length of string to minus -1 and assign it to the variable named 'last'
// return the variable last
