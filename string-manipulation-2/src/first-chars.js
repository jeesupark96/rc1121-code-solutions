/* exported firstChars */
// define a function named firstChars with two parameters length and string
// use the substring method to the string object with two arguments, 0 and length
// assign it to the variable newString and then return the newString

function firstChars(length, string) {
  var newString = string.substring(0, length);
  return newString;
}

// define a function named firstChars with two parameters length and string
// use the substring method to the string object with two arguments, 0 and length
// assign it to the variable newString and then return the newString
