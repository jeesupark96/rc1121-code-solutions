/* exported setValue */
// define function named 'setValue' with three parameters, 'object', 'key' and 'value'
// set the object with a dot notation to the key and assign it to the value
// return object

function setValue(object, key, value) {
  object[key] = value;
  return object;
}

// define function named 'setValue' with three parameters, 'object', 'key' and 'value'
// set the object with a bracket notation to the key and assign it to the value
// return object
