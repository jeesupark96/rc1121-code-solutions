/* exported getValue */
// define function named 'getValue' with two parameters, 'object' and 'key'
// call the parameter 'object' and using dot notation assign the 'key' parameter to it
// assign the result to the variable 'Value'
// return Value

function getValue(object, key) {
  var Value = object[key];
  return Value;
}

// define function named 'getValue' with two parameters, 'object' and 'key'
// call the parameter 'object' and using bracket notation assign the 'key' parameter to it
// assign the result to the variable 'Value'
// return Value
